@inherits ButtonComponent


<ButtonComponent Caption="@(Caption ?? "Edit")"
                 Color="@(Color ?? "secondary")"
                 Icon="@(Icon ?? "pencil")"
                 OnClick="OnClick"
                 ToolTipsStr="@(ToolTipsStr ?? "Edit")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />